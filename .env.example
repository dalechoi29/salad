NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key

# Optional: show a sticky amber bar on every page (leave empty on real production).
# Use a separate Supabase project for staging/local and point the two vars above there.
# Example: NEXT_PUBLIC_SITE_LABEL=Staging — test database
NEXT_PUBLIC_SITE_LABEL=
